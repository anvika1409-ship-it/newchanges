"""Deterministic policy logic (AI_DEVELOPMENT_RULES.md section 30: policies/).

Budget and authorization outcomes are deterministic and server-side. An
LLM cannot override a policy (rules sections 10 and 11)."""
