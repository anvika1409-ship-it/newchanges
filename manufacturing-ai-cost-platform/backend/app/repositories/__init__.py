"""Persistence abstraction (AI_DEVELOPMENT_RULES.md section 30: repositories/).

Business logic depends on repository interfaces, never on SQLite-specific
SQL, so the MVP database can be replaced without touching services."""
